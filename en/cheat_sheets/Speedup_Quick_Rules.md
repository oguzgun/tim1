# Speedup Quick Rules

1) don’t burn speedups outside events  
2) big jobs finish inside windows  
3) don’t spend to avoid idle time  
4) no spike target = no spend

Why:  
👉 ../community_insights/02_Speedup_Economy.md
